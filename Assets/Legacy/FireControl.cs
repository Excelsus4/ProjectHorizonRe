using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FireControl : MonoBehaviour {
	/*public MovementControl script;

	[HideInInspector]
	public bool isAuto;
	private bool isKeyDown;

	public void OnMDown()
	{
		script.FireTry();
		isKeyDown = true;
	}

	public void OnMUp()
	{
		isKeyDown = false;
	}

	private void FixedUpdate()
	{
		if (Input.GetButtonDown("FireMode") && isAuto)
			isAuto = false;
		else if (Input.GetButtonDown("FireMode") && !isAuto)
			isAuto = true;

		if (isAuto && isKeyDown)
			script.FireTry();
	}

	// Use this for initialization
	void Start () {
		isKeyDown = false;
		isAuto = false;
	}*/
}
